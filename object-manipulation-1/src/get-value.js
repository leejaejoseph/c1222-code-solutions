/* exported getValue */
/* define a function named getValue with 2 parameters (object and key)
return the value of the property key of object */
function getValue(object, key) {
  return object[key];
}
