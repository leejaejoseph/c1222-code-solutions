/* exported getLastChar */
/* defining function taking single parameter(string) with string data type
return out of function the last index of string by using its length - 1 for its index */

function getLastChar(string) {
  return string[string.length - 1];
}
