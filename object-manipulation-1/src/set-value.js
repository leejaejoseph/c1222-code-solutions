/* exported setValue */
/* define a function named setValue with 3 parameters (object, key, and value)
set the value of key in object to the value of value */
function setValue(object, key, value) {
  object[key] = value;
}
