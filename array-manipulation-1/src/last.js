/* exported last */
// define a function named last with one parameter(array)
function last(array) {
  // return the last index of the array by finding its length and minusing 1 for its index
  return array[array.length - 1];
}
