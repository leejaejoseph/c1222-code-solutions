/* exported getFirstChar */
// defining function taking single parameter(string) with string data type
function getFirstChar(string) {
  // return out of function the [zero index] of string
  return string[0];
}
